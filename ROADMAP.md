# Python 求职路线图 · 90 天计划

**目标**：找到 Python 相关工作（东莞 / 珠三角，可远程优先）
**节奏**：每天 2–4 小时，90 天 ≈ 3 个月
**总天数**：90 天（Day 01 起算，当前 Day 14）

---

## 四个阶段总览

| 阶段 | 天数 | 主题 | 产出 |
| --- | --- | --- | --- |
| ① 基础收尾 | Day 15–20 | 包与虚拟环境 / `pip` / 标准库 / 正则 / 调试 | 能管依赖、能自己读报错定位 |
| ② 从零做项目 | Day 21–35 | 白纸写项目 / `sqlite` 数据库 / 爬虫与 API | 2 个能写进简历的小项目 |
| ③ 求职核心技能 | Day 36–60 | Git 工作流 / Linux / HTTP / Web 或数据处理 / 测试 | 1 个完整后端或数据项目 |
| ④ 作品与求职 | Day 61–90 | 3 个作品打磨 / 简历 / 面试题 / 投递 | 简历 + 作品集 + 面试准备 |

---

## 阶段① 基础收尾（Day 15–20）

| 天 | 内容 | 为什么求职需要 |
| --- | --- | --- |
| 15 | 模块与包：`__init__.py`、包结构、`from .x import y` | 真实项目都是多文件的 |
| 16 | 虚拟环境与依赖：`venv`、`pip install`、`requirements.txt` | 面试必问；换机器能跑起来 |
| 17 | `pathlib`、`csv`、`datetime`、`collections`（`Counter`/`defaultdict`） | 标准库用得熟 = 写得快 |
| 18 | 正则表达式 `re`：匹配 / 提取 / 替换 | 爬虫、日志、清洗数据全靠它 |
| 19 | 调试：读 traceback、`print` 定位、`breakpoint()`、VS Code 断点 | 面试官爱问"你怎么排查问题" |
| 20 | 复习日：把通讯录从零重写一遍（不看原代码） | 检验是否真的会 |

**阶段①合格标准**：能自己建包、能 `pip` 装库照文档跑通例子、报错能自己定位。

---

## 阶段② 从零做项目（Day 21–35）

| 天 | 内容 | 产出 |
| --- | --- | --- |
| 21–23 | **项目①（从零）**：命令行记账本 —— 需求：记收支、按类别统计、导出 CSV、支持月度报表 | 一个 200 行左右的完整程序 |
| 24–26 | `sqlite3` 数据库：建表 / 增删改查 / SQL 基础 | 通讯录改造成数据库版 |
| 27–29 | 数据库进阶：`JOIN`、索引概念、用 Python 封装 DAO 层 | 会写"数据访问层" |
| 30–32 | 爬虫入门：`requests` + `BeautifulSoup`，抓静态网页、存库、去重、限速 | 数据采集能力 |
| 33–35 | **项目②**：定时抓取的行情/资讯小工具（抓 → 存 sqlite → 出日报） | 第二个简历项目 |

**阶段②合格标准**：给一个需求（含数据结构设计），能不看教程从零写出可运行程序。

---

## 阶段③ 求职核心技能（Day 36–60）

### 通用基本功（Day 36–42）

| 天 | 内容 |
| --- | --- |
| 36–37 | Git 工作流：分支、合并、冲突解决、PR、GitHub 仓库规范（README/`.gitignore`/许可证） |
| 38–39 | Linux 基础命令、SSH、进程与端口、日志查看（`tail`/`grep`） |
| 40 | HTTP 与 REST：方法、状态码、请求头、JSON API 设计 |
| 41–42 | 测试入门：`unittest`/`pytest` 写单元测试，`logging` 代替 `print` |

### 方向选择（Day 43 起二选一，推荐 A）

**方向 A · Python 后端（岗位最多，推荐）**

| 天 | 内容 |
| --- | --- |
| 43–46 | `FastAPI`（或 `Flask`）：路由、请求参数、返回 JSON、自动文档 |
| 47–50 | `MySQL`/`PostgreSQL` + `SQLAlchemy` ORM：模型、迁移、连接池 |
| 51–54 | 认证与会话：密码哈希、`JWT`、权限；分层结构（路由/服务/数据层） |
| 55–58 | **项目③**：一个带数据库的 REST 服务（如"待办+用户系统"），配 `pytest` 测试 |
| 59–60 | 部署：`Docker` 基础、`Nginx` 反向代理、云服务器上线 |

**方向 B · 数据处理 / 自动化（门槛较低，和你现有技能互补）**

| 天 | 内容 |
| --- | --- |
| 43–46 | `pandas`：读写 Excel/CSV、筛选分组、透视表 |
| 47–50 | 数据可视化：`matplotlib` / `plotly`，出图与报表 |
| 51–54 | SQL 分析：聚合、窗口函数、连表，接公司数据库跑报表 |
| 55–58 | **项目③**：一份从原始数据到图表报告的全流程脚本 |
| 59–60 | 自动化：定时任务（`schedule`/cron）、批量文件处理、Excel 自动化 |

**阶段③合格标准**：能写一个前后端能跑通、带数据库和测试的项目；能用 Git 做分支和 PR。

---

## 阶段④ 作品与求职（Day 61–90）

| 天 | 内容 |
| --- | --- |
| 61–66 | 作品打磨：3 个仓库（小工具 / 数据采集 / Web 服务），每个都要有清晰 README、截图、运行说明 |
| 67–70 | 简历：把项目写成"问题 → 方案 → 结果"，量化数据（抓了多少条、接口响应多少毫秒） |
| 71–75 | 面试基础：Python 特性（可变/不可变、深浅拷贝、装饰器、生成器）、数据结构与算法（列表/字典/栈/队列/排序/二分）、复杂度 |
| 76–80 | 数据库与网络面试题：SQL 手写、索引原理、HTTP/TCP 基础 |
| 81–85 | 手写代码训练：字符串/列表/字典类题目，`LeetCode` 简单+中等各 30 题 |
| 86–88 | 项目复盘：能把项目讲清楚（架构、难点、为什么这么设计） |
| 89–90 | 投递与模拟面试：目标岗位 30+ 家，记录反馈迭代 |

**阶段④合格标准**：3 个能当场打开演示的作品 + 简历 + 能扛住 30 分钟技术面。

---

## 每天怎么打卡

1. 写代码（不是读教程），至少 1 小时
2. 当天代码 `git commit` + `push`
3. 在 README 表格里把当天状态改成 ✅
4. 每 3 天：从零重写一次最近做过的东西，不看原文件
